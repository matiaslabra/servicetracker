// import { selectRoomListItemDomain } from '../selectors';

describe('selectRoomListItemDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
