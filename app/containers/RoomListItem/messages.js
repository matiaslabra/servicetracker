/*
 * RoomListItem Messages
 *
 * This contains all the text for the RoomListItem container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.RoomListItem';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the RoomListItem container!',
  },
});
